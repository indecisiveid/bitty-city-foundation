DEMO_ALLOWLIST=iosif.christian@sidejawn.io,iosif.christian@gmail.com,ccrimi75@gmail.com

# Cloud Storage bucket holding goal-proof photos (see functions/src/proofStorage.ts).
# Needed here because the emulator prompts for any param missing from .env,
# even one that has a default; prod uses the default.
PROOFS_BUCKET=bitty-city.firebasestorage.app
