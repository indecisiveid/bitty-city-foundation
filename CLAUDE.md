# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This directory is currently a Python 3.11 virtual environment (venv) with no application source code yet.

## Environment Setup

- Python 3.11.3 virtual environment
- Activate: `Scripts/activate` (Windows) or `source Scripts/activate` (Git Bash)
- Install dependencies: `pip install -r requirements.txt` (once a requirements file is created)
